name: Build and Deploy Docker Image

on:
  push:
    branches:
      - main
      - staging
      - "feature/**"
      - "patch/**"
      - "ci/**"
      - "doc/**"
      - "performance/**"
      - "refactor/**"
  pull_request:
    types: [closed]
    branches:
      - main
      - staging
      - "feature/**"
      - "patch/**"
      - "ci/**"
      - "doc/**"
      - "performance/**"
      - "refactor/**"
  workflow_dispatch:
env:
  DOCKER_REGISTRY: ${{ vars.DOCKER_REGISTRY }}
  DOCKER_IMAGE_NAME: ${{ vars.DOCKER_IMAGE_NAME }}
jobs:
  validate-and-build:
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history for proper version calculation

      - name: Validate branch name
        id: validate
        uses: ../actions/validate-branch

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            "$DOCKER_REGISTRY"/"$DOCKER_IMAGE_NAME"
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # - name: Create git tag for release branches
      #   if: success() && steps.validate.outputs.branch_type == 'release'
      #   uses: EnterpriseBT/ebt-docker-build-workflow/.github/actions/create-git-tag@main
      #   with:
      #     git_tag: ${{ steps.version.outputs.git_tag }}

      # - name: Create GitHub release
      #   id: github_release
      #   if: success() && steps.validate.outputs.branch_type == 'release'
      #   uses: EnterpriseBT/ebt-docker-build-workflow/.github/actions/create-github-release@main
      #   with:
      #     git_tag: ${{ steps.version.outputs.git_tag }}
      #     registry: ${{ inputs.registry }}
      #     image_name: ${{ inputs.image_name }}
      #     docker_tag: ${{ steps.version.outputs.docker_tag }}
      #     additional_tags: ${{ steps.version.outputs.additional_tags }}
      #     github_token: ${{ secrets.GITHUB_TOKEN }}

      # - name: Output summary
      #   id: output_summary
      #   if: success()
      #   uses: EnterpriseBT/ebt-docker-build-workflow/.github/actions/output-summary@main
      #   with:
      #     branch_name: ${{ steps.validate.outputs.branch_name }}
      #     branch_type: ${{ steps.validate.outputs.branch_type }}
      #     version: ${{ steps.version.outputs.version }}
      #     docker_tag: ${{ steps.version.outputs.docker_tag }}
      #     registry: ${{ inputs.registry }}
      #     image_name: ${{ inputs.image_name }}
      #     additional_tags: ${{ steps.version.outputs.additional_tags }}
      #     release_url: ${{ steps.github_release.outputs.release_url }}
